[phases.setup]
nixPkgs = ["python3", "ffmpeg", "gcc", "postgresql_16.dev", "freefont"]
# Cache-bust: 2026-01-15T17:45:00Z - Forces Railway rebuild


[phases.build]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "gunicorn app:app"
